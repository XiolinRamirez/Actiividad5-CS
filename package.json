{
  "name": "demo-ci",
  "version": "1.0.0",
  "private": true,
  "scripts": { "test": "jest --ci --coverage" },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "testMatch": ["**/*.test.js"],
    "testPathIgnorePatterns": ["/node_modules/", "/e2e/"]
  }
}
